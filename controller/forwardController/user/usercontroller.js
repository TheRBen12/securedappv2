class UserController {
    router

    register(method, url){
            }

    constructor(router) {
        this.router = router;
        this.register('post')
    }
}

module.exports = {UserController};
